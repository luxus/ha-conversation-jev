{
  "config": {
    "abort": {
      "already_configured": "Jev Assist is already configured.",
      "oauth_failed": "Grok sign-in was cancelled.",
      "reauth_successful": "Grok was reauthenticated."
    },
    "error": {
      "invalid_api_key": "Enter a non-empty API key.",
      "oauth_failed": "Grok sign-in failed. You can retry OAuth, use an API key, or abort.",
      "cannot_connect": "Could not reach the authentication server."
    },
    "progress": {
      "oauth_wait": "Sign in with Grok (uses Grok CLI OAuth client).\n\nOpen {url} and enter this code: **{user_code}**."
    },
    "step": {
      "user": {
        "title": "Jev Assist",
        "description": "TypeSafe/Jev uses an API key. Grok sign-in is OAuth by default (Grok CLI public client). An API key is only a fallback when OAuth entitlement is missing or you bill via console.x.ai.",
        "data": {
          "typesafe_api_key": "TypeSafe API key",
          "grok_auth_method": "Grok authentication"
        },
        "data_description": {
          "typesafe_api_key": "From the TypeSafe console. Stored in the Home Assistant config entry.",
          "grok_auth_method": "Primary: Sign in with Grok (uses Grok CLI OAuth client). Fallback: API key for https://api.x.ai."
        }
      },
      "oauth": {
        "title": "Sign in with Grok (uses Grok CLI OAuth client)"
      },
      "oauth_failed": {
        "title": "Grok sign-in did not complete",
        "description": "Device-code OAuth failed ({error}). Retry OAuth, use a Grok API key as a fallback, or abort.",
        "data": {
          "oauth_recovery": "Next step"
        }
      },
      "api_key": {
        "title": "Grok API key (fallback)",
        "description": "Use a console.x.ai API key when OAuth entitlement is missing or you bill via the developer API at https://api.x.ai. This is not the default.",
        "data": {
          "grok_api_key": "Grok API key"
        }
      },
      "reauth_confirm": {
        "title": "Reauthenticate Grok",
        "description": "Grok tokens are missing or were rejected. Sign in with Grok again (OAuth), or use an API key fallback.",
        "data": {
          "grok_auth_method": "Grok authentication"
        }
      }
    }
  },
  "selector": {
    "grok_auth_method": {
      "options": {
        "oauth": "Sign in with Grok (uses Grok CLI OAuth client)",
        "api_key": "Grok API key (console billing fallback)"
      }
    },
    "oauth_recovery": {
      "options": {
        "retry_oauth": "Retry Grok OAuth",
        "api_key": "Use a Grok API key",
        "abort": "Abort"
      }
    }
  }
}
